import { cors } from '../../lib/middleware';

export async function externalData(req, res) {
    // Handle preflight request
    // await cors(req, res);

  
    // const { data } = req.query;
  
    // // Your API logic here
    // // ...
    // console.log(data);
  
    // res.status(200).json({ message: 'Data received successfully!' });
  }
  